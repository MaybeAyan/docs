$accentColor = #a1c4fd                      // 主题颜色
$textColor = #2c3e50                        // 文本颜色
$borderColor = #eaecef                      // 边框线颜色
$codeBgColor = #282c34                      // 代码块背景色
$backgroundColor = #ffffff                  // 悬浮块背景色

// 全局样式的修改
body {
  font-family: 'Comic Sans MS',-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
}

.navbar{
  background: rgba(255,255,255,.2);
}
rgba(133, 104, 104, 0.2)
.home-blog .hero {
  margin:0 auto 0;
}

.navbar .links {
 background: rgba(255,255,255,.0);
 font-size: 1rem;
 font-weight: bold;
}
.search-box input {
  color: black;
  }

.reco-search:before {
  color: whitesmoke;
  }

.abstract-wrapper .iconfont {
  font-size: 15px
  }

文章视口
.abstract-item:hover {
  border-radius: 20px; 
  background-image: linear-gradient(to top, #dfe9f3 0%, white 100%);
  }

